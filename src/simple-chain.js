const { NotImplementedError } = require("../extensions/index.js");

/**
 * Implement chainMaker object according to task description
 *
 */
const chainMaker = {
  // chain: [],
  // getLength() {
  //   return this.chain.length;
  // },
  // addLink(value) {
  //   this.chain.push(toString(value));
  // },
  // removeLink(position) {
  //   this.chain.splice(position - 1, 1);
  // },
  // reverseChain() {},
  // finishChain() {},
};

module.exports = {
  chainMaker,
};
